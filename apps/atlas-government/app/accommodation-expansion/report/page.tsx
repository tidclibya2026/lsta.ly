"use client";import{useEffect,useState}from"react";import{expansionApi,EXPANSION_INTERNAL}from"@/lib/api/accommodation-expansion";import{isStaticDemo}from"@/lib/deployment-path";export default function Page(){const[data,setData]=useState<any>(null);useEffect(()=>{if(!isStaticDemo())void expansionApi.report().then(setData)},[]);if(isStaticDemo())return <p>{EXPANSION_INTERNAL}</p>;return <main><h1>تقرير التوسع</h1>{data&&<pre>{JSON.stringify(data.summary,null,2)}</pre>}</main>}
