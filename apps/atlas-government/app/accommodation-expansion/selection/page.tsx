"use client";import{useEffect,useState}from"react";import{expansionApi,EXPANSION_INTERNAL}from"@/lib/api/accommodation-expansion";import{isStaticDemo}from"@/lib/deployment-path";export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{if(!isStaticDemo())void expansionApi.selection().then(setRows)},[]);if(isStaticDemo())return <p>{EXPANSION_INTERNAL}</p>;return <main><h1>العينة المختارة</h1><p>{rows.length}/20</p><ul>{rows.map(x=><li key={x.proposal_id}>{x.name} — {x.safety_score}%</li>)}</ul></main>}
