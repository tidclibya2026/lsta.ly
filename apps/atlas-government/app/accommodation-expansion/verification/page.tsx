"use client";import{useEffect,useState}from"react";import{expansionApi,EXPANSION_INTERNAL}from"@/lib/api/accommodation-expansion";import{isStaticDemo}from"@/lib/deployment-path";export default function Page(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{if(!isStaticDemo())void expansionApi.verification().then(setRows)},[]);if(isStaticDemo())return <p>{EXPANSION_INTERNAL}</p>;return <main><h1>التحقق</h1><p>{rows.filter(x=>x.verification_status==="passed").length}/20 ناجح</p></main>}
