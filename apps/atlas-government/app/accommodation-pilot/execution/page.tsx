"use client";import{useEffect,useState}from"react";import{pilotApi,PILOT_INTERNAL_MESSAGE}from"@/lib/api/accommodation-pilot";import type{PilotExecutionBatch}from"@/lib/api/accommodation-pilot-types";import{isStaticDemo}from"@/lib/deployment-path";export default function Page(){const[b,setB]=useState<PilotExecutionBatch|null>(null),[error,setError]=useState("");const load=()=>pilotApi.getExecutionProgress().then(setB).catch(e=>setError(String(e)));useEffect(()=>{if(isStaticDemo())return;void load();if(b?.status!=="running")return;const id=setInterval(()=>void load(),5000);return()=>clearInterval(id)},[b?.status]);if(isStaticDemo())return <main><p>{PILOT_INTERNAL_MESSAGE}</p></main>;return <main><h1>تنفيذ Pilot</h1>{error?<p role="alert">{error}</p>:b?<><p>الحالة: {b.status}</p><p>العناصر: {b.items}</p><pre>{JSON.stringify(b.dry_run,null,2)}</pre><button disabled aria-label="تنفيذ Pilot">التنفيذ مكتمل</button></>:<p>جارٍ التحميل…</p>}</main>}
