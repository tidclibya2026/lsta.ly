"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{GovernmentShell}from"@/components/layout/GovernmentShell";import{ExecutionPreviewPanel}from"@/components/merge-execution/ExecutionPreviewPanel";import{mergeExecutionApi}from"@/lib/api/merge-execution";import type{ExecutionPreview}from"@/lib/api/merge-execution-types";import{isStaticDemo}from"@/lib/deployment-path";export default function Page(){const{id}=useParams<{id?:string}>(),params=useParams<{executionId:string}>(),[data,setData]=useState<ExecutionPreview|null>(null),[error,setError]=useState("");useEffect(()=>{if(isStaticDemo()||params.executionId==="internal")return;void mergeExecutionApi.storedPreview(params.executionId).then(setData).catch(e=>setError(String(e)))},[params.executionId]);if(isStaticDemo())return <GovernmentShell active="تنفيذ الدمج"><main><p>محرك تنفيذ الدمج النهائي متاح داخل البيئة الحكومية الداخلية فقط.</p></main></GovernmentShell>;return <GovernmentShell active="تنفيذ الدمج"><main className="mergePage"><h1>معاينة التنفيذ</h1>{error?<p role="alert">{error}</p>:data?<ExecutionPreviewPanel data={data}/>:<p>جارٍ التحميل…</p>}</main></GovernmentShell>}
