"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{GovernmentShell}from"@/components/layout/GovernmentShell";import{ExecutionWarningBanner}from"@/components/merge-execution/ExecutionWarningBanner";import{MergeExecutionSummary}from"@/components/merge-execution/MergeExecutionSummary";import{ExecutionBatchTable}from"@/components/merge-execution/ExecutionBatchTable";import{mergeExecutionApi}from"@/lib/api/merge-execution";import type{ExecutionBatch}from"@/lib/api/merge-execution-types";import{isStaticDemo}from"@/lib/deployment-path";
export default function Page(){const router=useRouter(),[rows,setRows]=useState<ExecutionBatch[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{if(isStaticDemo()){setLoading(false);return}void mergeExecutionApi.batches().then(setRows).catch(e=>setError(e instanceof Error?e.message:"تعذر التحميل")).finally(()=>setLoading(false))},[]);if(isStaticDemo())return <GovernmentShell active="تنفيذ الدمج"><main className="mergeInternalOnly"><h1>محرك تنفيذ الدمج النهائي</h1><p>محرك تنفيذ الدمج النهائي متاح داخل البيئة الحكومية الداخلية فقط.</p></main></GovernmentShell>;return <GovernmentShell active="تنفيذ الدمج"><main className="mergePage"><h1>محرك تنفيذ الدمج النهائي</h1><ExecutionWarningBanner/>{loading?<p>جارٍ التحميل…</p>:error?<p role="alert">{error}</p>:<><MergeExecutionSummary batches={rows}/><ExecutionBatchTable rows={rows}onOpen={id=>router.push(`/merge-execution/batches/${id}`)}/></>}</main></GovernmentShell>}
