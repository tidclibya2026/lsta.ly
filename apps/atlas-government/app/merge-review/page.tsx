"use client";import{useEffect,useState}from"react";import{MergeBatchCard}from"@/components/merge-review/MergeBatchCard";import{MergeReviewSummary}from"@/components/merge-review/MergeReviewSummary";import{GovernmentShell}from"@/components/layout/GovernmentShell";import{isStaticDemo}from"@/lib/deployment-path";import{mergeReviewApi,normalizeMergeReviewError}from"@/lib/api/merge-review";import type{MergeBatch,MergeSummary}from"@/lib/api/merge-review-types";import{useRouter}from"next/navigation";const empty:MergeSummary={batches:0,proposals:0,pending_review:0,ready_merge:0,needs_review:0,possible_match:0,high_conflicts:0,medium_conflicts:0,approved_merge:0,rejected_match:0,progress:{total:0,reviewed:0,pending:0,percentage:0}};export default function Page(){const router=useRouter(),[data,setData]=useState(empty),[batches,setBatches]=useState<MergeBatch[]>([]),[error,setError]=useState("");useEffect(()=>{if(isStaticDemo())return;void Promise.all([mergeReviewApi.summary(),mergeReviewApi.batches()]).then(([s,b])=>{setData(s);setBatches(b.items)}).catch(e=>setError(normalizeMergeReviewError(e)))},[]);if(isStaticDemo())return <GovernmentShell active="مراجعة الدمج"><main className="mergeInternalOnly"><div><h1>مراجعة دمج البيانات</h1><p>وحدة مراجعة دمج البيانات متاحة داخل البيئة الحكومية الداخلية فقط.</p></div></main></GovernmentShell>;return <GovernmentShell active="مراجعة الدمج"><main className="mergePage"><header className="mergeHeader"><small>National Spatial Merge Review Workspace</small><h1>مراجعة مطابقة Excel وKML</h1><p>لا دمج تلقائي ولا إنشاء لمواقع وطنية في هذه المرحلة.</p></header>{error&&<p role="alert">{error}</p>}<MergeReviewSummary data={data}/><p>تقدم المراجعة: {data.progress.percentage}%</p><section className="mergeBatches">{batches.map(x=><MergeBatchCard key={x.id} batch={x} onOpen={()=>router.push(`/merge-review/batches/${x.id}`)}/>)}</section></main></GovernmentShell>}
