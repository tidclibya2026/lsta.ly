"use client";
import { useParams } from "next/navigation";import { useEffect,useState } from "react";import { EmptyState } from "@/components/feedback/EmptyState";import { LoadingSpinner } from "@/components/feedback/LoadingSpinner";import { GovernmentShell } from "@/components/layout/GovernmentShell";import { PageHeader } from "@/components/layout/PageHeader";import { DatasetVersionsPanel } from "@/components/metadata/DatasetVersionsPanel";import { LineageGraph } from "@/components/metadata/LineageGraph";import { LineageTimeline } from "@/components/metadata/LineageTimeline";import { MetadataCompletenessPanel } from "@/components/metadata/MetadataCompletenessPanel";import { MetadataFieldsTable } from "@/components/metadata/MetadataFieldsTable";import { QualityRulesPanel } from "@/components/metadata/QualityRulesPanel";import { Card } from "@/components/ui/Card";import { Tabs } from "@/components/ui/Tabs";import { metadataApi } from "@/lib/api/metadata";import type { LineageGraph as Graph,MetadataEntry } from "@/lib/api/metadata-types";
export default function MetadataDetail(){const{catalogCode}=useParams<{catalogCode:string}>();const[entry,setEntry]=useState<MetadataEntry|null>(null);const[graph,setGraph]=useState<Graph>({nodes:[],edges:[]});const[loading,setLoading]=useState(true);useEffect(()=>{Promise.all([metadataApi.entry(catalogCode),metadataApi.lineage("source_file",catalogCode)]).then(([e,g])=>{setEntry(e);setGraph(g)}).finally(()=>setLoading(false))},[catalogCode]);if(loading)return <GovernmentShell><LoadingSpinner/></GovernmentShell>;if(!entry)return <GovernmentShell><EmptyState title="الأصل الوصفي غير موجود"/></GovernmentShell>;const overview=<div className="reviewDetailGrid"><Card><h3>بيانات الأصل</h3><dl><dt>الرمز</dt><dd>{entry.catalog_code}</dd><dt>النوع</dt><dd>{entry.entry_type}</dd><dt>التصنيف</dt><dd>{entry.classification_level}</dd><dt>دورة الحياة</dt><dd>{entry.lifecycle_status}</dd></dl></Card><Card><MetadataCompletenessPanel value={entry.completeness}/></Card></div>;return <GovernmentShell active="فهرس البيانات"><div className="reviewPage"><PageHeader eyebrow={entry.catalog_code} title={entry.title_ar}/><Tabs ariaLabel="تفاصيل الأصل الوصفي" items={[{id:"overview",label:"نظرة عامة",content:overview},{id:"fields",label:"الحقول",content:<MetadataFieldsTable items={[]}/>},{id:"versions",label:"الإصدارات",content:<DatasetVersionsPanel items={[]}/>},{id:"lineage",label:"مسار البيانات",content:<><LineageGraph graph={graph}/><LineageTimeline items={graph.edges}/></>},{id:"quality",label:"الجودة",content:<QualityRulesPanel items={[]}/>},{id:"audit",label:"التدقيق",content:<EmptyState title="سجل التدقيق متاح داخل البيئة الحكومية"/>}]}/></div></GovernmentShell>}
