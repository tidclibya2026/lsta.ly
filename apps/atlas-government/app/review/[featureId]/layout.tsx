export const dynamicParams = false;
export function generateStaticParams() { return [{ featureId: "internal" }]; }
export default function ReviewFeatureLayout({ children }: { children: React.ReactNode }) { return children; }
