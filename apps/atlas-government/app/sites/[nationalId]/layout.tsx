export const dynamicParams = false;
export function generateStaticParams() { return [{ nationalId: "LSTA-OLD-TRIPOLI-000001" }]; }
export default function SiteLayout({ children }: { children: React.ReactNode }) { return children; }
