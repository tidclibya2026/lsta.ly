export function MetricBar({ label, value, max, displayValue }: { label: string; value: number; max: number; displayValue?: string }) { const percent = max > 0 ? Math.min(100, Math.max(0, (value / max) * 100)) : 0; return <div className="dsMetricBar"><span>{label}</span><div role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={max} aria-valuenow={value}><i style={{ width: `${percent}%` }} /></div><b>{displayValue ?? value.toLocaleString("ar-LY")}</b></div>; }
