import type { DiscoveryFilters as Filters } from "@/lib/api/discovery-types";
export function DiscoveryFilters({value,onChange}:{value:Filters;onChange:(value:Filters)=>void}){return <aside className="discoveryPanel"><h2>الفلاتر</h2><label>المصدر<select value={value.source} onChange={e=>onChange({...value,source:e.target.value as Filters["source"],offset:0})}><option value="all">الكل</option><option value="registry">السجل الوطني</option><option value="staging">قيد المراجعة</option></select></label><label>نوع الهندسة<select value={value.geometry_type||""} onChange={e=>onChange({...value,geometry_type:e.target.value||undefined,offset:0})}><option value="">الكل</option><option>Point</option><option>LineString</option><option>Polygon</option></select></label><label><input type="checkbox" checked={value.has_images===true} onChange={e=>onChange({...value,has_images:e.target.checked?true:undefined,offset:0})}/> يحتوي صورًا</label></aside>}
