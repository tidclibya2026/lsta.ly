export function DiscoveryPagination({offset,limit,total,onChange}:{offset:number;limit:number;total:number;onChange:(offset:number)=>void}){return <nav className="discoveryPagination" aria-label="صفحات النتائج"><button disabled={!offset} onClick={()=>onChange(Math.max(0,offset-limit))}>السابق</button><span>{Math.floor(offset/limit)+1}</span><button disabled={offset+limit>=total} onClick={()=>onChange(offset+limit)}>التالي</button></nav>}
