import Link from "next/link"; import type { DiscoveryResult } from "@/lib/api/discovery-types";
export function DiscoveryResultCard({item}:{item:DiscoveryResult}){return <article className={`discoveryResult ${item.source}`}><header><span>{item.source==="registry"?"السجل الوطني":"بيانات قيد المراجعة"}</span><b>{item.relevance_score}</b></header><h2>{item.name_ar||"عنصر بلا اسم"}</h2><p>{item.description_excerpt||"لا يوجد وصف مختصر."}</p><small>{item.geometry_type} · {item.municipality||"بلدية غير محددة"} · جودة {item.quality_score??"—"}</small>{item.is_review_data&&<div className="reviewNotice">غير معتمد للنشر العام</div>}<Link href={item.detail_url}>فتح</Link></article>}
