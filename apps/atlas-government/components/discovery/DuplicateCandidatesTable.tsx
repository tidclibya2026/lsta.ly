import type{DuplicateCandidate}from "@/lib/api/discovery-types";export function DuplicateCandidatesTable({items,onSelect}:{items:DuplicateCandidate[];onSelect?:(item:DuplicateCandidate)=>void}){return <div className="dsTableWrap"><table className="dsTable"><thead><tr><th>المصدر</th><th>الهدف</th><th>الثقة</th><th>الحالة</th></tr></thead><tbody>{items.map(item=><tr key={item.id} onClick={()=>onSelect?.(item)}><td>{item.source_entity_id}</td><td>{item.target_entity_id}</td><td>{item.confidence_score}%</td><td>{item.status}</td></tr>)}</tbody></table></div>}
