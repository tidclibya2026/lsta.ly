import type{DuplicateCandidate}from "@/lib/api/discovery-types";export function DuplicateComparisonPanel({item}:{item:DuplicateCandidate|null}){return item?<section className="discoveryPanel"><h2>معاينة المقارنة</h2><div className="comparison"><code>{item.source_entity_id}</code><code>{item.target_entity_id}</code></div><p>درجة الثقة {item.confidence_score}% — لا يتم أي دمج في هذه المرحلة.</p></section>:null}
