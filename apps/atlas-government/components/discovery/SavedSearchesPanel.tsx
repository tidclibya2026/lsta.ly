import type{SavedSearch}from "@/lib/api/discovery-types";export function SavedSearchesPanel({items,onRun}:{items:SavedSearch[];onRun?:(item:SavedSearch)=>void}){return <div className="discoveryPanel">{items.length?items.map(item=><article key={item.id}><h3>{item.query_name}</h3><p>{item.query_text}</p>{onRun&&<button onClick={()=>onRun(item)}>تشغيل</button>}</article>):<p>لا توجد عمليات بحث محفوظة.</p>}</div>}
