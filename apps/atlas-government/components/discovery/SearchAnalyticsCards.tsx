import type{DiscoveryAnalytics}from "@/lib/api/discovery-types";export function SearchAnalyticsCards({data}:{data:DiscoveryAnalytics}){return <div className="analyticsCards">{[["بحث اليوم",data.searches_today],["هذا الأسبوع",data.searches_this_week],["متوسط الزمن",`${data.average_query_time_ms} ms`],["بحث محفوظ",data.saved_searches_count]].map(([label,value])=><article className="dsCard" key={label}><span>{label}</span><strong>{value}</strong></article>)}</div>}
