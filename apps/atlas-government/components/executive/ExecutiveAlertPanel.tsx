import type{ExecutiveAlert}from"@/lib/api/executive-types";export function ExecutiveAlertPanel({items}:{items:ExecutiveAlert[]}){return <section className="executivePanel"><h2>التنبيهات</h2>{items.length?items.map(x=><article key={x.id} className={`alert-${x.severity}`}><b>{x.title_ar}</b><p>{x.description_ar}</p><small>{x.status}</small></article>):<p>لا توجد تنبيهات متاحة.</p>}</section>}
