export function ExecutiveTrendChart({label,values}:{label:string;values:number[]}){const max=Math.max(...values,1);return <figure className="executiveTrend"><figcaption>{label}</figcaption><svg viewBox="0 0 300 80" role="img"><polyline fill="none" stroke="#d7a83c" strokeWidth="4" points={values.map((v,i)=>`${i*300/Math.max(values.length-1,1)},${75-v*70/max}`).join(" ")}/></svg></figure>}
