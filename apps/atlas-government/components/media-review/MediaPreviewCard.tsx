"use client";import { useState } from "react";import { ImageGallery } from "@/components/data-display/ImageGallery";import { Button } from "@/components/ui/Button";import { Card } from "@/components/ui/Card";import type { MediaReviewItem } from "@/lib/api/media-review-types";export function MediaPreviewCard({item}:{item:MediaReviewItem}){const[show,setShow]=useState(false);return <Card><h3>معاينة آمنة</h3>{show?<ImageGallery images={[{local_media_url:item.local_media_url,original_url:item.normalized_url,rights_status:item.rights_status as never}]} alt={item.site_name||"صورة الموقع"}/>:<Button variant="secondary" onClick={()=>setShow(true)}>تحميل المعاينة عند الطلب</Button>}</Card>}
