import Link from "next/link";import { DataTable,type DataColumn } from "@/components/data-display/DataTable";import type { MediaReviewItem } from "@/lib/api/media-review-types";import { MediaSourceBadge } from "./MediaSourceBadge";import { MediaStatusBadge } from "./MediaStatusBadge";export function MediaReviewTable({items}:{items:MediaReviewItem[]}){const columns:DataColumn<MediaReviewItem>[]=[{key:"site",header:"الموقع",render:i=><><strong>{i.site_name||"بلا اسم"}</strong><small>{i.feature_id}</small></>},{key:"source",header:"المصدر",render:i=><MediaSourceBadge source={i.source_type}/>},{key:"review",header:"المراجعة",render:i=><MediaStatusBadge status={i.review_status}/>},{key:"rights",header:"الحقوق",render:i=><MediaStatusBadge status={i.rights_status}/>},{key:"open",header:"",render:i=><Link href={`/media-review/${i.id}`} className="reviewOpen">فتح</Link>}];return <DataTable rows={items} columns={columns} rowKey={i=>i.id}/>}
