"use client";import{useState}from"react";export function ExecutionAuthorizationPanel({enabled,onExecute}:{enabled:boolean;onExecute:(text:string)=>void}){const[text,setText]=useState("");return <section><h2>التفويض النهائي</h2><label>اكتب EXECUTE APPROVED MERGES<input value={text}onChange={e=>setText(e.target.value)}/></label><button disabled={!enabled||text!=="EXECUTE APPROVED MERGES"}onClick={()=>onExecute(text)}>تنفيذ الدمج المعتمد</button></section>}
