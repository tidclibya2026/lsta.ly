import type{ExecutionBatch}from"@/lib/api/merge-execution-types";export function ExecutionBatchTable({rows,onOpen}:{rows:ExecutionBatch[];onOpen:(id:string)=>void}){if(!rows.length)return <p>لا توجد دفعات تنفيذ. لم يُنفذ أي دمج حقيقي.</p>;return <table><thead><tr><th>الرمز</th><th>الحالة</th><th>المطلوب</th><th>المؤهل</th><th>الإجراء</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.execution_code}</td><td>{x.status}</td><td>{x.requested_proposal_count}</td><td>{x.eligible_proposal_count}</td><td><button onClick={()=>onOpen(x.id)}>فتح</button></td></tr>)}</tbody></table>}
