import type{ExecutionBatch}from"@/lib/api/merge-execution-types";export function MergeExecutionSummary({batches}:{batches:ExecutionBatch[]}){const total=batches.reduce((n,b)=>n+b.requested_proposal_count,0);return <section className="mergeExecutionGrid" aria-label="ملخص التنفيذ"><div><b>{batches.length}</b><span>دفعات التنفيذ</span></div><div><b>{total}</b><span>المقترحات</span></div><div><b>{batches.filter(x=>x.status==="blocked").length}</b><span>دفعات محظورة</span></div></section>}
