import type{MergeSummary}from"@/lib/api/merge-review-types";export function MergeReviewSummary({data}:{data:MergeSummary}){const entries=[["إجمالي المقترحات",data.proposals],["قيد المراجعة",data.pending_review],["جاهز للمراجعة",data.ready_merge],["يحتاج مراجعة",data.needs_review],["مطابقة محتملة",data.possible_match],["تعارض مرتفع",data.high_conflicts],["معتمد للمرحلة التالية",data.approved_merge],["مرفوض",data.rejected_match]];return <section className="mergeSummary">{entries.map(([x,v])=><article className="dsCard" key={x}><span>{x}</span><strong>{v}</strong></article>)}</section>}
