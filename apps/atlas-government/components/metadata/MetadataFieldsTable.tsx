import { EmptyState } from "@/components/feedback/EmptyState";export function MetadataFieldsTable({items}:{items:unknown[]}){return items.length?<pre>{JSON.stringify(items,null,2)}</pre>:<EmptyState title="لا توجد حقول موثقة"/>}
