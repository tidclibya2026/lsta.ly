import { SearchInput } from "@/components/forms/SearchInput";import { Select } from "@/components/forms/Select";import type { MetadataFilters as Filters } from "@/lib/api/metadata-types";
export function MetadataFilters({value,onChange}:{value:Filters;onChange:(v:Filters)=>void}){return <section className="reviewFilters"><SearchInput label="البحث" value={value.search||""} onChange={e=>onChange({...value,search:e.target.value})}/><Select label="نوع الأصل" value={value.entry_type||""} onChange={e=>onChange({...value,entry_type:e.target.value})} options={[{value:"",label:"كل الأنواع"},{value:"dataset",label:"مجموعة بيانات"},{value:"source_file",label:"ملف مصدر"},{value:"api_endpoint",label:"API"},{value:"map_layer",label:"طبقة خريطة"},{value:"registry",label:"سجل وطني"}]}/></section>}
