import { Dialog } from "@/components/ui/Dialog"; import { Button } from "@/components/ui/Button"; import type { SiteRelationship } from "@/lib/api/registry-types";
export function RelationshipVerifyDialog({item,onDecision,onClose}:{item:SiteRelationship|null;onDecision:(decision:"verify"|"reject")=>void;onClose:()=>void}){return <Dialog open={item!==null} title="مراجعة العلاقة المكانية" onClose={onClose}><p>{item?.target_name||item?.target_id}</p><div className="reviewActions"><Button onClick={()=>onDecision("verify")}>اعتماد</Button><Button variant="danger" onClick={()=>onDecision("reject")}>رفض</Button></div></Dialog>}
