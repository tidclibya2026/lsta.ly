import { DataTable, type DataColumn } from "@/components/data-display/DataTable"; import { EmptyState } from "@/components/feedback/EmptyState"; import type { SiteDocument } from "@/lib/api/registry-types";
export function SiteDocumentsPanel({ items }: { items: SiteDocument[] }) { const columns: DataColumn<SiteDocument>[] = [{ key:"title", header:"الوثيقة", render:x=>x.title_ar },{ key:"type", header:"النوع", render:x=>x.document_type },{ key:"file", header:"الملف", render:x=>x.file_name },{ key:"status", header:"الحالة", render:x=>x.verification_status }]; return items.length ? <DataTable rows={items} columns={columns} rowKey={x=>x.id} caption="وثائق الموقع" /> : <EmptyState title="لا توجد وثائق" />; }
