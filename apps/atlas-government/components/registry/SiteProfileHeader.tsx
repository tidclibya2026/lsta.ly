import { Badge } from "@/components/ui/Badge"; import { Button } from "@/components/ui/Button"; import type { RegistryDetails } from "@/lib/api/registry-types";
export function SiteProfileHeader({ site, canEdit, onEdit }: { site: RegistryDetails; canEdit: boolean; onEdit: () => void }) { return <header className="dsPageHeader"><div><p>{site.national_id}</p><h1>{site.name_ar}</h1><div className="registryHeaderMeta"><span>{site.name_en||"—"}</span><Badge tone={site.verification_status==="approved"?"success":"warning"}>{site.verification_status}</Badge><Badge tone="neutral">{site.publication_status}</Badge><strong>{site.completeness.score}% مكتمل</strong></div></div><div className="reviewActions">{canEdit&&<Button onClick={onEdit}>تحرير</Button>}<Button variant="secondary" onClick={()=>window.print()}>طباعة</Button></div></header>; }
