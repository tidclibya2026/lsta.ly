import { Checkbox } from "@/components/forms/Checkbox";import { Select } from "@/components/forms/Select";import type { SearchFilters as Values } from "@/lib/api/search-types";
export function SearchFilters({value,onChange}:{value:Values;onChange:(value:Values)=>void}){return <aside className="searchFilters"><h2>تصفية النتائج</h2><Select label="المصدر" value={value.source} onChange={e=>onChange({...value,source:e.target.value,offset:0})} options={[{value:"all",label:"الكل"},{value:"registry",label:"السجل الوطني"},{value:"staging",label:"قيد المراجعة"}]}/><Select label="الهندسة" value={value.geometry_type||""} onChange={e=>onChange({...value,geometry_type:e.target.value,offset:0})} options={[{value:"",label:"كل الهندسات"},{value:"Point",label:"Point"},{value:"LineString",label:"LineString"},{value:"Polygon",label:"Polygon"}]}/><Select label="الحالة" value={value.verification_status||""} onChange={e=>onChange({...value,verification_status:e.target.value,offset:0})} options={[{value:"",label:"كل الحالات"},{value:"approved",label:"معتمد"},{value:"pending_review",label:"قيد المراجعة"}]}/><Checkbox label="المسماة فقط" checked={value.has_name||false} onChange={e=>onChange({...value,has_name:e.target.checked||undefined,offset:0})}/><Checkbox label="بها صور" checked={value.has_images||false} onChange={e=>onChange({...value,has_images:e.target.checked||undefined,offset:0})}/></aside>}
