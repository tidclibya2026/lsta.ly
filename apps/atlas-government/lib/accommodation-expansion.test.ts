import test from"node:test";import assert from"node:assert/strict";import{canExecuteExpansion,validExpansionCount,EXPANSION_INTERNAL}from"./api/accommodation-expansion";test("exactly twenty",()=>{assert.equal(validExpansionCount(20),true);assert.equal(validExpansionCount(19),false);assert.equal(validExpansionCount(21),false)});test("execution gate",()=>{assert.equal(canExecuteExpansion(20,0,"GO","EXECUTE CONTROLLED 20 HOTEL EXPANSION"),true);assert.equal(canExecuteExpansion(20,1,"GO","EXECUTE CONTROLLED 20 HOTEL EXPANSION"),false)});test("static message is safe",()=>assert.equal(EXPANSION_INTERNAL.includes("proposal"),false));
