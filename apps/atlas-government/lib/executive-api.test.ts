import assert from"node:assert/strict";import test from"node:test";import{normalizeExecutiveError}from"./api/executive";test("يوحد أخطاء لوحة القيادة",()=>assert.match(normalizeExecutiveError(null),/البيانات التنفيذية/));test("بيانات funnel لا تعرض published الصفري",()=>{const f={imported:430,promoted:1,published:0};assert.equal(Object.entries(f).filter(([k,v])=>k!=="published"||v>0).length,2)});
